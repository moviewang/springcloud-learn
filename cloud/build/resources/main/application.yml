spring:
  application:
    name: cloud-server
server:
  port: 9003
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:9003/eureka/

